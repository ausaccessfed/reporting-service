= breadcrumbs({ 'Dashboard' => dashboard_path },
                'Daily Demand Report')

h1 Daily Demand Report

p Shows daily demand report for the whole federation over the last 12 months, in number of Sessions

= render(partial: 'shared/report_output', locals: { data: @data })
