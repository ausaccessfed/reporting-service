= breadcrumbs({ 'Admin Reports' => admin_reports_path },
                'Federated Sessions Report')

h1 Federated Sessions Report

p Shows the sessions of the whole federation within a time range, in number of Sessions

= render(partial: 'form')
